<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rainmtime.com.demorepo">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".test_code.asmr_test.viewdraghelper.ViewDragHelperActivity" />

        <activity android:name=".test_code.nestedRecyclerView.NestedActivity" />
        <activity android:name=".test_code.TestActivity" />

        <service android:name=".test_code.inentservice.TestIntentService" />

        <activity android:name=".test_code.stickerbubbleanimtest.StickerBubbleActivity" />
    </application>

</manifest>